#include <stdio.h>
int main(){
    // 尚未解決
    int t,a,b,len;
    int arr[]={};
    scanf("%d",&t);
    int i=1;
    while(i<=t){
        scanf("%d %d",&a,&b);




        printf("%d\n",sizeof (arr)/sizeof (arr[0]));
        i++;
    }

    return 0;
}